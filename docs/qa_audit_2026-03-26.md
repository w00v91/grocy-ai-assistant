# QA Audit – 2026-03-26

## Durchgeführte Prüfungen

- `source .venv/bin/activate && pytest` → **PASS** (354 Tests)
- `source .venv/bin/activate && ruff check .` → **PASS**
- `source .venv/bin/activate && black --check .` → **PASS**
- `source .venv/bin/activate && node --test tests/frontend/*.mjs` → **FAIL** (2 Frontend-Tests)

## Gefundene Probleme

### Frontend-Testfehler

1. **`tests/frontend/test_native_shopping_swipe.mjs`**
   - Fehler: Regex-Assertion erwartet weiterhin den direkten Aufruf
     `bindShoppingImageFallbacks(this); this._rebindSwipeInteractions();`
   - Ist-Zustand: Implementierung ist offenbar geändert/umstrukturiert, Test erwartet veraltetes Snippet.
   - Empfehlung: Test robuster auf Verhalten statt exakten Quelltext matchen (z. B. DOM-/Event-Outcome prüfen).

2. **`tests/frontend/test_shopping_search_focus_retention.mjs`**
   - Fehler: `TypeError: Cannot read properties of undefined (reading 'firstElementChild')`
   - Stelle: `GrocyAIShoppingSearchBar._createVariantCard` in `grocy-ai-dashboard.js`.
   - Empfehlung: Defensive Guards in `_createVariantCard` ergänzen (Fallback bei fehlenden DOM-Knoten) und Testdaten auf erwartete Struktur prüfen.

## Vorschläge für README-/Markdown-Updates

1. **README: Abschnitt „Vollständige lokale Prüfung“ präzisieren**
   - Ergänzen, dass `node --test tests/frontend/*.mjs` derzeit auf CI-Laufzeit/Implementierungsstand reagieren kann und als separater Frontend-Block betrachtet werden sollte.

2. **README: Fehlerbehebungsabschnitt für Frontend-Tests ergänzen**
   - Kurzer Troubleshooting-Block mit typischen Fehlerbildern:
     - Regex-basierte Snapshot-/Codepattern-Tests bei Refactorings
     - DOM-Null-Guards für Variant-Rendering

3. **ARCHITECTURE.md: Test-Matrix ergänzen**
   - API-/Unit-/Frontend-Testarten als eigene Tabelle mit Verantwortlichkeiten (Backend vs. Panel-Frontend).

4. **docs/notification_architecture.md: Querverweis auf aktuelle Panel-Spezifikation prüfen**
   - Optionalen „Siehe auch“-Block einfügen, der auf `NOTIFICATION_DASHBOARD_SPEC.md` verweist, falls dort UI-Verhalten detaillierter beschrieben ist.

5. **README + DOCS.md: Versionshinweis automatisierungsnah formulieren**
   - Klarstellen, dass Version in `config.yaml`, `manifest.json`, `const.py`, Frontend-Konstante und Top-Changelog synchron sein muss (inkl. kurzem Check-Kommando).

